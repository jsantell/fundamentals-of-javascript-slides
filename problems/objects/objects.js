/**
 * Make an array of objects. Each object should represent a person.
 * Each object should have 2-3 properties, such as 'name' or 'age'.
 * Iterate over all of these objects and print out each property and value
 * of that object.
 */
